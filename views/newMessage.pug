extends layouts/main-layout.pug

block content 

    .chatPageContainer 
        .chatTitleBar 
            label(for="userSearchTextbox") To: 
            #selectedUsers 
                input#userSearchTextbox(type="text", placeholder="Who you want to message?")
        .resultsContainer
        button#createChatButton(disabled) Create a new chat